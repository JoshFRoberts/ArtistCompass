/**
 * Base types
 */

import Input from './Input.vue';
import Select from './Select.vue';
import Radio from './Radio.vue';
import RadioGroup from './RadioGroup.vue';
import CheckboxGroup from './CheckboxGroup.vue';
import Checkbox from './Checkbox.vue';
import Textarea from './Textarea.vue';

export {
    Input as colorType,
    Input as dateType,
    Input as datetimeLocalType,
    Input as fileType,
    Input as imageType,
    Input as hiddenType,
    Input as monthType,
    Input as numberType,
    Input as passwordType,
    Input as rangeType,
    Input as resetType,
    Input as submitType,
    Input as emailType,
    Input as telType,
    Input as textType,
    Input as urlType,
    Input as weekType,
    Input as searchType,
    Select as selectType,
    Input as timeType,
    Radio as radioType,
    RadioGroup as radioGroupType,
    CheckboxGroup as checkboxGroupType,
    CheckboxGroup as toggleGroupType,
    Checkbox as checkboxType,
    Checkbox as toggleType,
    Textarea as textareaType,
}
